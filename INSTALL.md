# INSTALL — AIエージェント向け導入手順

> このファイルは**AIエージェントが読んで実行する**ための手順書です。
> 人間向けの説明は [README.md](README.md) にあります。
> 利用者から「このリポジトリを導入して」と言われたら、このファイルの手順を上から順に実行してください。

導入は5分・ファイルのコピーと追記だけで完了します。ビルドもインストールも不要です。

---

## 手順0：作業フォルダと常時読み込みファイルを確認する

1. 導入先は**利用者が今いる作業フォルダ**（カレントディレクトリ）。別の場所には作らない
2. 常時読み込みファイルを、自分が動いているツールに合わせて決める

| ツール | ファイル |
|---|---|
| Claude Code / Cowork | `CLAUDE.md` |
| Codex / GitHub Copilot CLI | `AGENTS.md` |
| Cursor | `.cursor/rules/library.mdc` |
| その他 | そのツールが毎回自動で読むファイル。無ければ `AGENTS.md` |

以降これを **`<常時ファイル>`** と呼びます。

3. `library/` が既にあるか確認する。**あれば上書きせず、利用者に「既存の library があります。中に統合しますか、別名にしますか」と聞いて止まる**

## 手順1：ファイルを取得する

次のどちらかで、このリポジトリの中身を一時フォルダに取得します。

```
git clone https://github.com/fuyutosan/daitoshokan-ai-library.git
```

`git` が使えない場合は、以下のファイルを raw から individually 取得してください（`main` ブランチ）。

```
library/目録.md
library/運用ルール.md
library/日誌/README.md
library/棚/仕事.md
library/棚/趣味・創作.md
library/棚/AI活用術.md
library/棚/自分の好み・判断基準.md
snippets/claude-md-snippet.md
commands/log.md
commands/tidy.md
```

raw の URL は `https://raw.githubusercontent.com/fuyutosan/daitoshokan-ai-library/main/<パス>` です。

## 手順2：library/ を作業フォルダに置く

取得した `library/` フォルダを、作業フォルダの直下にそのままコピーします。

```
作業フォルダ/
├── library/
│   ├── 目録.md
│   ├── 運用ルール.md
│   ├── 棚/（4冊）
│   └── 日誌/README.md
└── <常時ファイル>
```

## 手順3：`<常時ファイル>` にスニペットを追記する

`snippets/claude-md-snippet.md` の中の ```markdown ブロックの**中身だけ**を、`<常時ファイル>` の末尾に追記します。
`<常時ファイル>` が無ければ新規作成します。**既存の内容は消さないこと。**

追記する内容（このまま貼ってよい）:

```markdown
## 📚 大図書館（作業コンテキストの書庫）

**読む** — 過去の経緯や決めた理由が要る仕事・同じプロジェクトの続き・提案や企画のときだけ `library/目録.md` を見て、関係する棚を1〜2冊だけ開く。棚は全文読まず、検索（Grep）で見出しやキーワードを引いて必要な部分だけ読む。単発の質問やその場で終わる作業では開かない。

**書く** — ファイルを作った・変えた作業が一区切りついたら `library/日誌/YYYY-MM.md` に5行以内で追記する。見出しは必ず
`## 2026-08-05 やったこと ｜棚:棚の名前`
の形にする（`｜棚:` が無いと整理されず、永久に棚へ上がらない）。棚の名前は `library/目録.md` の表にあるものを使う。

**整理** — 日誌に追記する前に、その月の日誌の見出し行だけを検索する（`^## ` で検索。本文は読まない）。`✅` の付いていない見出しが**5件以上**あれば、`library/運用ルール.md` の「4. 整理の手順」に従って棚へ転記し、済んだ見出しに `✅` を付ける。**ユーザーに頼まれなくても自分でやる。**
```

## 手順4：スラッシュコマンドを置く（Claude Code のみ・任意）

Claude Code なら、`commands/log.md` と `commands/tidy.md` を `.claude/commands/` にコピーします。
これで利用者が `/log`（今の作業を日誌に書く）と `/tidy`（今すぐ棚へ整理する）を手動でも呼べます。
自動運用だけで足りるので、無くても動きます。他のツールならこの手順は飛ばしてください。

## 手順5：利用者に合わせて棚を作り直す

**ここが一番大事です。** 初期の4冊（仕事／趣味・創作／AI活用術／自分の好み・判断基準）は例なので、利用者に合わせます。

1. 利用者に**3問だけ**質問する
   - 主にどんな仕事・プロジェクトにAIを使いますか（2〜3個）
   - 趣味や個人的な制作でもAIを使いますか
   - 過去の経緯を思い出してほしい場面はどれですか
2. 答えに合わせて `library/棚/` のファイル名を変える・増やす・減らす（3〜5冊が目安。多すぎると選べない）
3. `library/目録.md` の「棚の案内」表を、実際の棚ファイルと**完全に一致**させる
   （ここがズレると整理の転記先が決まらず、自動化が止まる）
4. 使わない棚のサンプル（`⚠️ ここから下はサンプルです` より下）は削除する

## 手順6：動くか確認して報告する

以下を実際に確かめてから報告してください。

- [ ] `library/目録.md` の棚一覧と、`library/棚/` の実ファイルが一致している
- [ ] `<常時ファイル>` にスニペットが入っている
- [ ] `library/日誌/` に今月のファイル（`YYYY-MM.md`）を作り、この導入作業自体を1件目のエントリとして書いた
      （見出しは `## 2026-08-05 大図書館を導入した ｜棚:◯◯` の形。これが型のお手本になります）

最後に、次の内容を利用者に報告してください。

- どこに何を作ったか（ファイル一覧）
- 棚を何冊、どんな名前にしたか
- **「今後は普通に作業してください。区切りがついたら私が日誌に書き、5件たまったら私が棚へ整理します」**

---

## 導入後の動き方（エージェント向けの補足）

- 日誌を書くのは「ファイルを作った・変えた作業が一区切りついたとき」。雑談や単発の質問では書かない
- 整理は日誌に追記する**直前**に判定する。判定は見出し行を検索するだけなので安い
- 利用者が「ログ」「記録して」と言ったら、条件を待たずに日誌を書く
- 棚が育ってきたら、目録の1行説明も実態に合わせて直す
