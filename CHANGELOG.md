# 変更履歴

## Unreleased

- 公開テンプレと個人用 `library/` を区別する安全ゲートを追加
- 秘密情報・個人情報・生ログを記録しないルールを追加
- 複数AI向けのlibrary外stagingと単一昇格経路を文書化
- Markdownリンク、棚一覧、しきい値、スニペットを検査する標準ライブラリ製validatorを追加
- validatorに秘密情報スキャン（APIキー・トークン・秘密鍵・メール・電話番号）を追加。値は出力せず種類と場所だけを報告する
- validatorの必須ファイルに、スニペット・運用ルール・コマンドを追加（欠落しても合格する状態を修正）
- しきい値チェックが「✅の付いていない」表記を拾えず、スニペット側のズレを見逃していた問題を修正
- 運用ルールとスニペットに「書き手は1つだけ」を明記し、複数AI運用の文書へ導線を追加
- INSTALLの公開状態の確認に具体的なコマンドと、導入完了チェック項目を追加
- validatorを利用者の library にも実行できるようパス引数に対応
- GitHub ActionsをNode 24対応版（checkout@v5 / setup-python@v6）へ更新
- テストをテンポラリ複製上で実行するよう変更（中断しても作業ツリーが壊れない）。8件→16件
- 安全ルールを「安全契約ブロック」として原文で固定し、可視本文に1文字ずつ一致するかを検査するよう変更。
  日本語の意味は判定しない。同義の言い換えも不合格になる（仕様）。
  削除・反転・緩和に加え、HTMLコメント・コードブロック（字下げ形式を含む）・引用・raw HTMLへの退避、契約行への継ぎ足しを検出する。
  別行に矛盾する許可文を足す攻撃は列挙による網で拾うが、網羅は主張しない
- 禁止文の補助検査にもNFKC正規化を適用し、全角英数の許可文を検出する
- 契約の内容そのものをテストで固定ハッシュ照合し、静かな書き換えを検出する
- モードの自動判定を廃止。`INSTALL.md` や `snippets/` を消して弱いモードへ落とす回避を塞いだ。
  CIは `--mode template` を明示する
- `library/` をMarkdown専用とし、非Markdownを不合格にする。あわせて `library/` 配下の
  テキストファイルは拡張子によらず秘密情報を走査し、UTF-8として読めないファイルは不合格にする
- 電話番号の検出をハイフンなし・空白区切り・括弧・国番号・全角形式へ拡張し、誤検知しない形も回帰テストで固定
- `--instructions-file` で常時読み込みファイルの契約2行を検査できるようにし、
  未指定時は「library本体のみ検査」と明記する
- 秘密情報スキャンにOpenAIプロジェクトキー（`sk-proj-`）とGitHub fine-grained PAT（`github_pat_`）を追加
- 検証をtemplate（公開テンプレ全体）とinstalled（導入済みlibraryのみ）の2モードに分離。
  Python APIの既定はtemplate。CLIはパス省略でtemplate、パス指定でinstalled、`--mode`指定を最優先する
- `docs/` を `library/docs/` へ移動。導入先へ一緒にコピーされ、運用ルールからの参照が切れなくなった
- テスト複製をROOT.parent配下のUUID付きフォルダ方式に変更（tempfile非依存）。16件→36件
- Markdown表示判定・UTF-8強制・電話番号形式の回帰テストを追加。56件→62件
